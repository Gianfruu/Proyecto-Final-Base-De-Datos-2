<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Detalle del Movimiento</title>
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
</head>
<body>
<body>
  <%- include('../partials/navbar') %>
  
  <div class="container">
    <h1>Detalle del Movimiento</h1>
    <a href="/movimientos?page=<%= page %>&limit=<%= limit %>&keyword=<%= keyword %>&sort=<%= sort %>&order=<%= order %>" class="back-link">← Volver a Movimientos</a>

    <ul> 
      <li><strong>Id movimiento:</strong> <%= movimiento._id %></li>     
      <li><strong>Producto:</strong> <%= movimiento.productoId.nombre %> (<%= movimiento.productoId.codigo %>)</li>
      <li><strong>Id de producto:</strong> <%= movimiento.productoId._id %></li>
      <li><strong>Tipo:</strong> <%= movimiento.tipo %></li>
      <li><strong>Cantidad:</strong> <%= movimiento.cantidad %></li>
      <li><strong>Motivo:</strong> <%= movimiento.motivo %></li>      
      <li><strong>Usuario:</strong> <%= movimiento.usuario %></li>
      <li><strong>Fecha:</strong> 
        <%= new Date(movimiento.fecha).toLocaleString('es-AR', {
          day: '2-digit',
          month: '2-digit',
          year: 'numeric',
          hour: '2-digit',
          minute: '2-digit'
        }) %>
      </li>
      
      

    </ul>
  </div>
</body>
</html>
